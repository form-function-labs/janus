"""Janus — a gated, validated, staged optimizer over Claude Code's bounded
markdown text-state (memory, skills, ``CLAUDE.md``).

Two faces: one reads the past (finished transcripts), one shapes the future
(the next session's context). Nothing live changes until you adopt.
"""

from __future__ import annotations

__version__ = "0.2.0"
